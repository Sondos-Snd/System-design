[default]
aws_access_key_id = YOUR_ACCESS_KEY
aws_secret_access_key = YOUR_SECRET_KEY


Configure AWS credentials to access DynamoDB. Create a file named credentials in the ~/.aws directory on your machine
If you’re running this in AWS (e.g., EC2, Lambda), it will use the IAM role assigned to the instance.